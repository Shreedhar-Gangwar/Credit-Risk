# --- Data: never commit raw (large, ~2.6 GB) or generated/processed ---
# Raw CSVs are obtained from the Home Credit dataset; see README for the source.
data/raw/
data/processed/

# --- Model + pipeline artifacts (regenerated by run_pipeline.py) ---
models/artifacts/
*.joblib
*.pkl

# --- Reports: keep writeups (.md) AND figures (small, portfolio-visible on GitHub) ---
# figures are regenerable via run_pipeline.py but committed so reports render online.

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.ipynb_checkpoints/
.pytest_cache/
.venv/
venv/
env/

# --- OS / editor ---
.DS_Store
Thumbs.db
.vscode/
.idea/
